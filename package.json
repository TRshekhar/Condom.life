{
  "name": "condom-life",
  "version": "1.0.0",
  "private": true,
  "description": "condom.life — Public Opinion Platform",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && cd frontend && npm install --save-dev tailwindcss autoprefixer postcss",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm start --prefix frontend",
    "start:backend": "npm start --prefix backend",
    "start:frontend": "npm start --prefix frontend"
  }
}